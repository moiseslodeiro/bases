/**
 * ¡No dejes que sepa que tienes prisa por imprimir! Disponemos de una impresora
 * con 3 colores de tinta, cyan, magenta y amarillo (CMY). Disponemos de una
 * función printer que recibe un objeto con los 3 colores como clave y un número
 * de 0 a 1000 como valor de la cantidad restante de tinta de ese color.
 *
 * Ejemplo:
 * {
 *    cyan: 244,
 *    magenta: 85,
 *    yellow: 128
 * }
 *
 * Adicionalmente sabemos que cada página consume un color de cada uno en cada
 * impresión (es decir, una de cyan, una de magenta y una unidad de amarillo).
 *
 * Tu deber es calcular cuántas páginas puede imprimir la impresora con un
 * cartucho.
 *
 *
 */
function printer(cartridge) {
  // Inicio código
  // Fin código
}
module.exports = printer
